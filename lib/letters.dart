import 'package:flutter/material.dart';

void main() => runApp(new Photography());

class Photography extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
